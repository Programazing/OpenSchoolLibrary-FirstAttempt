@model IEnumerable<Open_School_Library.Data.Entities.Setting>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FineAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FineAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SettingID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SettingID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SettingID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
